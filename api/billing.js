const { neon } = require('@neondatabase/serverless');
const { getSessionUser, parseCookies, COOKIE_NAME } = require('./auth');
const DEFAULT_PRICE_ID='price_1UFUSRQShPftL8gJSrU4djc5';
const PACKS={50:{credits:50,amount:149},150:{credits:150,amount:299},500:{credits:500,amount:799}};
function send(res,s,b){res.status(s).json(b)}
function dbUrl(){return process.env.DATABASE_URL||process.env.POSTGRES_URL||process.env.DATABASE_URL_UNPOOLED||''}
function body(req){if(!req.body)return{};if(typeof req.body==='string'){try{return JSON.parse(req.body)}catch{return{}}}return req.body}
function origin(req){const p=String(req.headers['x-forwarded-proto']||'https').split(',')[0].trim(),h=String(req.headers['x-forwarded-host']||req.headers.host||'').split(',')[0].trim();return h?`${p}://${h}`:'https://rightsradaruk.vercel.app'}
function sameOrigin(req){return !req.headers.origin||req.headers.origin===origin(req)}
async function currentUser(sql,req){return getSessionUser(sql,parseCookies(req.headers.cookie||'')[COOKIE_NAME]||'')}
async function schema(sql){await sql`CREATE TABLE IF NOT EXISTS ai_assistant_entitlements(user_id TEXT PRIMARY KEY,plan TEXT NOT NULL DEFAULT 'free',status TEXT NOT NULL DEFAULT 'active',stripe_customer_id TEXT,stripe_subscription_id TEXT,current_period_end TIMESTAMPTZ,updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW())`;await sql`CREATE TABLE IF NOT EXISTS ai_credit_balances(user_id TEXT PRIMARY KEY,credits INTEGER NOT NULL DEFAULT 0 CHECK(credits>=0),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW())`;await sql`CREATE TABLE IF NOT EXISTS ai_credit_purchases(stripe_session_id TEXT PRIMARY KEY,user_id TEXT NOT NULL,credits INTEGER NOT NULL,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW())`}
function form(p,k,v){if(v==null)return;if(Array.isArray(v))return v.forEach((x,i)=>form(p,`${k}[${i}]`,x));if(typeof v==='object')return Object.entries(v).forEach(([x,y])=>form(p,k?`${k}[${x}]`:x,y));p.append(k,String(v))}
async function stripe(path,payload={},method='POST'){const key=process.env.STRIPE_SECRET_KEY||'';if(!key)throw Object.assign(new Error('STRIPE_NOT_CONFIGURED'),{status:503});const f=new URLSearchParams();Object.entries(payload).forEach(([k,v])=>form(f,k,v));const r=await fetch(`https://api.stripe.com/v1/${path}${method==='GET'&&f.size?'?'+f:''}`,{method,headers:{authorization:`Bearer ${key}`,...(method==='POST'?{'content-type':'application/x-www-form-urlencoded'}:{})},body:method==='POST'?f.toString():undefined});const d=await r.json().catch(()=>({}));if(!r.ok)throw Object.assign(new Error(d.error?.message||'Stripe request failed.'),{status:r.status});return d}
async function customer(sql,u,existing){if(existing.customerId)return existing.customerId;const c=await stripe('customers',{email:u.email||undefined,name:u.name||undefined,metadata:{rightsradar_user_id:u.id}});await sql`INSERT INTO ai_assistant_entitlements(user_id,plan,status,stripe_customer_id,updated_at) VALUES(${u.id},'free','active',${c.id},NOW()) ON CONFLICT(user_id) DO UPDATE SET stripe_customer_id=EXCLUDED.stripe_customer_id,updated_at=NOW()`;return c.id}
module.exports=async function(req,res){res.setHeader('Cache-Control','no-store');if(!['GET','POST'].includes(req.method))return send(res,405,{error:'Method not allowed.'});const conn=dbUrl();if(!conn)return send(res,503,{error:'Account database is not connected.'});const sql=neon(conn);try{await schema(sql);const u=await currentUser(sql,req).catch(()=>null);if(!u)return send(res,401,{error:'Sign in to manage RightsRadar Plus.',code:'SIGN_IN_REQUIRED'});const rr=await sql`SELECT plan,status,stripe_customer_id AS "customerId",stripe_subscription_id AS "subscriptionId",current_period_end AS "currentPeriodEnd" FROM ai_assistant_entitlements WHERE user_id=${u.id} LIMIT 1`;const e=rr[0]||{plan:'free',status:'active',customerId:null};const bb=await sql`SELECT credits FROM ai_credit_balances WHERE user_id=${u.id} LIMIT 1`;const topupCredits=Number(bb[0]?.credits||0);if(req.method==='GET')return send(res,200,{ok:true,plan:e.plan,status:e.status,currentPeriodEnd:e.currentPeriodEnd||null,topupCredits,billingConfigured:Boolean(process.env.STRIPE_SECRET_KEY),priceId:process.env.STRIPE_PRICE_ID||DEFAULT_PRICE_ID,testMode:process.env.STRIPE_LIVE_MODE!=='true',packs:Object.values(PACKS)});if(!sameOrigin(req))return send(res,403,{error:'Invalid request origin.'});const b=body(req),action=String(b.action||'');if(action==='checkout'){if(e.plan==='plus'&&['active','trialing'].includes(String(e.status)))return send(res,409,{error:'RightsRadar Plus is already active.'});const cid=await customer(sql,u,e);const s=await stripe('checkout/sessions',{mode:'subscription',customer:cid,client_reference_id:u.id,success_url:`${origin(req)}/assistant.html?checkout=success&session_id={CHECKOUT_SESSION_ID}`,cancel_url:`${origin(req)}/assistant.html?checkout=cancelled`,line_items:[{price:process.env.STRIPE_PRICE_ID||DEFAULT_PRICE_ID,quantity:1}],metadata:{rightsradar_user_id:u.id},subscription_data:{metadata:{rightsradar_user_id:u.id}}});return send(res,200,{ok:true,url:s.url})}if(action==='topup'){const pack=PACKS[Number(b.credits)];if(!pack)return send(res,400,{error:'Choose a valid credit pack.'});const cid=await customer(sql,u,e);const s=await stripe('checkout/sessions',{mode:'payment',customer:cid,client_reference_id:u.id,success_url:`${origin(req)}/assistant.html?topup=success&session_id={CHECKOUT_SESSION_ID}`,cancel_url:`${origin(req)}/assistant.html?topup=cancelled`,line_items:[{price_data:{currency:'gbp',unit_amount:pack.amount,product_data:{name:`RightsRadar AI — ${pack.credits} credits`}},quantity:1}],metadata:{rightsradar_user_id:u.id,rightsradar_type:'ai_topup',credits:String(pack.credits)}});return send(res,200,{ok:true,url:s.url})}if(action==='portal'){if(!e.customerId)return send(res,400,{error:'No Stripe customer is connected yet.'});const p=await stripe('billing_portal/sessions',{customer:e.customerId,return_url:`${origin(req)}/assistant.html`});return send(res,200,{ok:true,url:p.url})}if(action==='sync'){const id=String(b.sessionId||'').trim();if(!/^cs_[A-Za-z0-9_]+$/.test(id))return send(res,400,{error:'Invalid Checkout session.'});const s=await stripe(`checkout/sessions/${encodeURIComponent(id)}`,{expand:['subscription']},'GET');if(String(s.client_reference_id||s.metadata?.rightsradar_user_id||'')!==String(u.id))return send(res,403,{error:'Checkout session does not belong to this account.'});if(s.mode==='payment'&&s.payment_status==='paid'&&s.metadata?.rightsradar_type==='ai_topup'){const credits=Number(s.metadata.credits||0);if(!PACKS[credits])return send(res,400,{error:'Invalid credit purchase.'});const ins=await sql`INSERT INTO ai_credit_purchases(stripe_session_id,user_id,credits) VALUES(${s.id},${u.id},${credits}) ON CONFLICT(stripe_session_id) DO NOTHING RETURNING stripe_session_id`;if(ins.length)await sql`INSERT INTO ai_credit_balances(user_id,credits,updated_at) VALUES(${u.id},${credits},NOW()) ON CONFLICT(user_id) DO UPDATE SET credits=ai_credit_balances.credits+EXCLUDED.credits,updated_at=NOW()`;const bal=await sql`SELECT credits FROM ai_credit_balances WHERE user_id=${u.id}`;return send(res,200,{ok:true,type:'topup',credited:Boolean(ins.length),topupCredits:Number(bal[0]?.credits||0)})}const sub=typeof s.subscription==='object'?s.subscription:null;if(!sub)return send(res,409,{error:'Subscription is not ready yet.'});const end=Number(sub.current_period_end||0)?new Date(Number(sub.current_period_end)*1000).toISOString():null;await sql`INSERT INTO ai_assistant_entitlements(user_id,plan,status,stripe_customer_id,stripe_subscription_id,current_period_end,updated_at) VALUES(${u.id},'plus',${String(sub.status||'incomplete')},${String(s.customer||e.customerId||'')},${String(sub.id||'')},${end},NOW()) ON CONFLICT(user_id) DO UPDATE SET plan='plus',status=EXCLUDED.status,stripe_customer_id=EXCLUDED.stripe_customer_id,stripe_subscription_id=EXCLUDED.stripe_subscription_id,current_period_end=EXCLUDED.current_period_end,updated_at=NOW()`;return send(res,200,{ok:true,active:['active','trialing'].includes(String(sub.status)),status:sub.status})}return send(res,400,{error:'Unknown billing action.'})}catch(error){if(error.message==='STRIPE_NOT_CONFIGURED')return send(res,503,{error:'Stripe billing is not configured.',code:'BILLING_NOT_CONFIGURED'});console.error('billing request failed',error);const s=Number(error.status)||500;return send(res,s>=400&&s<500?s:500,{error:s>=400&&s<500?error.message:'Unable to manage billing right now.'})}};
